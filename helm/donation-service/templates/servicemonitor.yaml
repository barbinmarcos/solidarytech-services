apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: donation-service
  namespace: monitoring
  labels:
    release: monitoring

spec:
  namespaceSelector:
    matchNames:
      - solidarytech

  selector:
    matchLabels:
      app: donation-service

  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
